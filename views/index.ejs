<%- include("partials/header.ejs") %>
    <% if (!locals.posts) { %>
        <h1> Hello my dear world!</h1>
    <% } else { %>
        <% for(let i = 0; i < posts.length; i+= 1) { %>
            <div class="card">

                <h5 class="card-header"><%= posts[i][0] %></h5>
                <div class="card-body">
                <p class="card-text"><%= posts[i][1] %></p>
                <form action="./submit-edit" method="POST">
                    <input type="hidden" name="title" value="<%= posts[i][0] %>"/>
                    <input type="hidden" name="content" value="<%= posts[i][1] %>"/>
                    <input type="hidden" name="num" value="<%= i %>"/>
                    <button type="submit" class="btn btn-primary" >Edit</a>
                </form>

                <form action="./submit-delete" method="POST">
                    <input type="hidden" name="num" value="<%= i %>"/>
                    <button class="btn btn-danger">Delete</button>
                </form>
                </div>
            </div>
        <% } %>
    <% } %>
<%- include("partials/footer.ejs") %>